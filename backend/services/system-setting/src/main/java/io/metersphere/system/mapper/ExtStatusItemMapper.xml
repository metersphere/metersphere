<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.metersphere.system.mapper.ExtStatusItemMapper">

    <select id="getStatusItemIdByRefId" resultType="java.lang.String">
        SELECT id FROM status_item WHERE ref_id = #{refId}
    </select>
</mapper>