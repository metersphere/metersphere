<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.metersphere.base.mapper.ext.ExtCustomFieldMapper">

    <sql id="condition">
        <include refid="io.metersphere.base.mapper.ext.ExtCustomFieldMapper.condition"/>
    </sql>

    <select id="list" resultType="io.metersphere.base.domain.CustomField">
        select
        <include refid="io.metersphere.base.mapper.CustomFieldMapper.Base_Column_List"/>,
        <include refid="io.metersphere.base.mapper.CustomFieldMapper.Blob_Column_List"/>
        from custom_field
        <include refid="queryWhereCondition"/>
        <include refid="io.metersphere.base.mapper.ext.ExtBaseMapper.orders"/>
    </select>

    <sql id="queryWhereCondition">
        <where>
            <if test="request.name != null">
                and custom_field.name LIKE CONCAT('%', #{request.name}, '%')
            </if>

            <if test="request.workspaceId != null">
                AND custom_field.workspace_id = #{request.workspaceId}
            </if>

            <if test="request.filters != null and request.filters.size() > 0">
                <foreach collection="request.filters.entrySet()" index="key" item="values">
                    <if test="values != null and values.size() > 0">
                        <choose>
                            <when test="key == 'scene'">
                                AND custom_field.scene IN
                                <foreach collection="values" item="value" separator="," open="(" close=")">
                                    #{value}
                                </foreach>
                            </when>
                            <when test="key == 'type'">
                                AND custom_field.type IN
                                <foreach collection="values" item="value" separator="," open="(" close=")">
                                    #{value}
                                </foreach>
                            </when>
                        </choose>
                    </if>
                </foreach>
            </if>
        </where>
    </sql>


    <!--    <sql id="combine">-->
<!--        <if test='${condition}.name != null and (${name} == null or ${name} == "")'>-->
<!--            AND load_test.name-->
<!--            <include refid="condition">-->
<!--                <property name="object" value="${condition}.name"/>-->
<!--            </include>-->
<!--        </if>-->
<!--    </sql>-->

<!--    <select id="list" resultMap="BaseResultMap"-->
<!--            parameterType="io.metersphere.performance.request.QueryTestPlanRequest">-->
<!--        SELECT load_test.*, project.name AS project_name, user.name AS user_name-->
<!--        FROM load_test-->
<!--        LEFT JOIN project ON load_test.project_id = project.id-->
<!--        LEFT JOIN user ON load_test.user_id = user.id-->
<!--    </select>-->

</mapper>
