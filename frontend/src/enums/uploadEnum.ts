export enum UploadAcceptEnum {
  excel = '.xlsx,.xls',
  word = '.docx,.doc',
  pdf = '.pdf',
  txt = '.txt',
  vedio = '.mp4',
  sql = '.sql',
  csv = '.csv',
  zip = '.zip',
  xmind = '.xmind',
  image = '.jpg,.png',
}

export default UploadAcceptEnum;
