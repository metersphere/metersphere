export enum UploadAcceptEnum {
  excel = '.xlsx,.xls',
  word = '.docx,.doc',
  pdf = '.pdf',
  txt = '.txt',
  video = '.mp4',
  sql = '.sql',
  csv = '.csv',
  zip = '.zip',
  xmind = '.xmind',
  image = '.jpg,.jpeg,.png,.svg',
  jar = '.jar',
  none = 'none',
}

export default UploadAcceptEnum;
