export interface UserGroupState {
  // 当前用户组名字
  currentName: string;
  currentTitle: string;
  currentId: string;
  // 当前用户类型
  currentType: string;
  // 菜单开启关闭
  collapse: boolean;
  currentInternal: boolean;
}
